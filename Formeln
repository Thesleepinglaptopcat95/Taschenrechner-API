///Formeln 



///Addition Zahlen und Terme
function addition(x, z) {
  return x + z;
} 



///Subtraktion Zahlen und Terme

function subtraction(x, z) {
    return x - z;
  } 

///Multiplikation von Zahlen und Termen
  function multiplikation(x, z) {
    return x * z;
  } 

//Dividieren von Zahlen und Termen

function division(x, z) {
    return x / z;
  } 

// // Addition von Termen komplexen Zahlen  ===> noch nicht funktionsfähig
// function complexAddition(terms) {
//     let result = {
//       real: 0,
//       imag: 0
//     };
  
//     terms.forEach(term => {
//       result.real += term.real;
//       result.imag += term.imag
//     });
  
//     return result;
//   }

//  ///Subtraktion zum lösen von Termen mit komplexen Zahlen 
 
// function subtractComplex(...terms) {
//     let result = terms[0];
//   for (let i = 1; i < terms.length; i++) {
//       result = {
//         real: result.real - terms[i].real,
//         imaginary: result.imaginary - terms[i].imaginary
//       };
//     }
  

//     return result;
//   }


//  ///Multiplikation zum lösen von Termen mit komplexer Zahlen
//  function multiplyComplex(a, b) {
//     let real = a.real * b.real - a.imag * b.imag;
//     let imag = a.real * b.imag + a.imag * b.real;
//     return {real, imag};
//   }
  
//   function multiplyNComplex(terms) {
//     let result = terms[0];
//     for (let i = 1; i < terms.length; i++) {
//       result = multiplyComplex(result, terms[i]);
//     }
//     return result;
//   }

// // Division zum lösen von Termen mit komplexer Zahlen
// const divideComplex = (terms) => {
//     let result = terms[0];
//     for (let i = 1; i < terms.length; i++) {
//       let denom = terms[i].real * terms[i].real + terms[i].imag * terms[i].imag;
//       result = {
//         real: result.real * terms[i].real + result.imag * terms[i].imag,
//         imag: result.imag * terms[i].real - result.real * terms[i].imag
//       }
//       result = {
//         real: result.real / denom,
//         imag: result.imag / denom
//       }
//     }
//     return result;
//   };
